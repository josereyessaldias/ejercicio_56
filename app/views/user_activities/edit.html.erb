<div class="agregar-info col-6">
	<h4><%= current_user.name %> <%= @activity.activity.category.past %> <%= @activity.activity.name %>.</h4>
	<p>Agregue más información sobre su actividad.</p>

	<img src="<%= @activity.activity.photo.normal %>" ><br><br>
	<div class="useract_fecha">
		<% if user_signed_in? && can?(:update, @activity) %>
			<%= form_with model: UserActivity, url: update_user_activity_path(@user), method: :patch, local: true do |f| %>

				<%= f.label :fecha_actividad %>
				<%= f.text_field :start, id: :event_start %><br><br>

				<%= f.label :agregar_foto_personal %><br>
		    	<%= f.file_field :photo %> <br><br>

				<%= f.submit "Enviar" %><br><br>

			<% end %>
		<% end %>
	</div>

</div>